<!doctype html>
<html lang="en">
    <%- include('./partials/head', { title })%>
    <body>
        <%- include('./partials/nav') %>
        <div class="p-5">
            <button type="button" class="btn btn-success" data-bs-toggle="modal" data-bs-target="#exampleModal">
                Add Store
            </button>
            <table class="table">
                <thead>
                    <tr>
                        <th scope="col">Store ID</th>
                        <th scope="col">Name</th>
                        <th scope="col">Location</th>
                        <th scope="col">Phone</th>
                        <th scope="col">Email</th>
                    </tr>
                </thead>
                <tbody>
                    <% stores.forEach((item) => { %>
                        <tr>
                            <th><%= item.id  %></th>
                            <td><%= item.name  %></td>
                            <td><%= item.location  %></td>
                            <td><%= item.phone  %></td>
                            <td><%= item.email  %></td>
                            <td>
                                <button type="button" class="btn btn-warning" data-bs-toggle="modal" data-bs-target=<%= "#" + item.id %>>
                                    <i class="bi bi-pencil-square"></i>
                                </button>
                            </td>
                            <%- include('./partials/storeUpdateModal.ejs', { formName: "storeUpdateForm", store: item }) %>
                        </tr>
                    <% }); %>
                </tbody>
            </table>
        </div>
        <%- include('./partials/storeAddModal.ejs', { formName: 'storeAddForm' })%>
    </body>
</html>
